diff --git a/lib/client.js b/lib/client.js
index dc95f6cd0442d343086711c159d19d44771b9665..02dfbbc1b3942dd3f0890bd54dab363188f1c839 100644
--- a/lib/client.js
+++ b/lib/client.js
@@ -688,6 +688,7 @@ window.__ModuleLoader__.load({
 			return next;
 		}
 		const STORAGE_PREFIX = "dsh-sidebar:v1";
+		const DRAFT_SESSION_ID = "__dsh_better_sidebar_draft__";
 		/** Default panel width for one viewport: the prefs percent of the window,
 		* clamped to the panel floor (a tiny percent must stay usable) and to the
 		* viewport (a large one must never cover the whole window). */
@@ -705,6 +706,7 @@ window.__ModuleLoader__.load({
 				}
 			} catch {}
 			const viewport = typeof window !== "undefined" ? window.innerWidth : void 0;
+			if (sessionId === DRAFT_SESSION_ID) return makeDefaultState(viewport === void 0 ? 400 : defaultWidthFor(viewport, prefs.defaultWidthPercent), false, prefs.tabsEnabled["explorer"] !== false);
 			return makeDefaultState(viewport === void 0 ? 400 : defaultWidthFor(viewport, prefs.defaultWidthPercent), prefs.openByDefault && (viewport === void 0 || !isNarrowWidth(viewport)), prefs.tabsEnabled["explorer"] !== false);
 		}
 		/**
@@ -1955,7 +1957,7 @@ window.__ModuleLoader__.load({
 		}
 		/** Resolve a (possibly relative) path against the session cwd for the sidebar. */
 		function resolveSidebarPath(cwd, path) {
-			if (path.startsWith("/") || /^[A-Za-z]:[\\/]/.test(path)) return path;
+			if (path.startsWith("/") || path.startsWith("\\\\") || /^[A-Za-z]:[\\/]/.test(path)) return path;
 			const base = cwd ?? "";
 			if (base === "") return path;
 			const separator = base.includes("\\") ? "\\" : "/";
@@ -6813,7 +6815,7 @@ window.__ModuleLoader__.load({
 			const current = sessionList.current;
 			const snapshot = (0, react.useSyncExternalStore)((0, react.useCallback)((callback) => store.subscribe(callback), [store]), (0, react.useCallback)(() => store.getSnapshot(), [store]));
 			(0, react.useEffect)(() => {
-				store.setSession(current);
+				store.setSession(current ?? DRAFT_SESSION_ID);
 			}, [current, store]);
 			const state = snapshot.state;
 			const sessionId = snapshot.sessionId;
@@ -7080,7 +7082,7 @@ window.__ModuleLoader__.load({
 			*/
 			const bottomWasOpenRef = (0, react.useRef)(void 0);
 			(0, react.useEffect)(() => {
-				if (narrow) return;
+				if (narrow || current === void 0) return;
 				if (state === void 0) return;
 				const wasOpen = bottomWasOpenRef.current;
 				bottomWasOpenRef.current = state.bottomOpen;
@@ -7098,7 +7100,8 @@ window.__ModuleLoader__.load({
 				state,
 				store,
 				ctx,
-				narrow
+				narrow,
+				current
 			]);
 			const panelRef = (0, react.useRef)(null);
 			const bottomRef = (0, react.useRef)(null);
@@ -8756,12 +8759,13 @@ window.__ModuleLoader__.load({
 		//#endregion
 		//#region \0dsh-css:/Users/menghuan/Code/DSH-better-sidebar/src/client/layout.css.mjs
 		const css = "/**\n * Layout push: when a panel is open it OCCUPIES the layout instead of\n * floating over it — the app shell (#root, the AppFrame three-column grid)\n * gives up space. Only the center column is flexible (1fr), so the right\n * panel's width squeeze (margin-right on #root) lands exactly on the\n * conversation output and the input bar, like a VSCode sidebar.\n *\n * The bottom panel squeezes ONLY the center column — it must not cover the\n * app's own left sidebar or the right panel. DSH 0.1.x wraps slot hosts in\n * [data-slot] containers, so the AppFrame grid lives one level deeper:\n * #root > div[data-slot=\"root\"] > div (the frame). Its grid items are\n * (frame > div:nth-child(1..3)): sidebarCol, centerCol, detailsCol, so the\n * vertical push lands on the center column alone. The fixed panels cover\n * the vacated strips, which looks seamless because #root's background is\n * the theme base.\n *\n * The sizes ride CSS variables updated by the Sidebar shell (0 while\n * collapsed); expand/collapse animates both the margins and the panel\n * slides on the same theme duration. Drags disable the transition so the\n * layout tracks the pointer.\n */\n#root {\n  margin-right: var(--dsh-sidebar-width, 0px);\n  transition: margin-right var(--ds-transition-duration-slow) var(--ds-ease-in-out);\n}\n\n/* The AppFrame's grid items are sidebarCol, centerCol, detailsCol (children\n   1-3 of #root > div[data-slot=\"root\"] > div) — nth-child(2) is the center\n   column. A stretched grid item shrinks by its margins, so the conversation\n   content (output + input bar) lifts without touching the sidebars. */\n#root > div[data-slot=\"root\"] > div > div:nth-child(2) {\n  margin-bottom: var(--dsh-sidebar-height, 0px);\n  transition: margin-bottom var(--ds-transition-duration-slow) var(--ds-ease-in-out);\n}\n\n/* When the sidebar is collapsed, the toggle cluster reclaims the top-right\n   corner. Push the DSH session header's right padding out so its right-aligned\n   utilities (the \"Session log\" download capsule) yield the corner instead of\n   hiding under the cluster. The header default right-pads 28px; the 2-button\n   cluster spans right 10→70px, so 78px clears it with an 8px gap. Anchor on\n   the header's slot host wrapper ([data-slot=\"conversation.session.header\"])\n   rather than a positional path: DSH 0.1.x nests the header several levels\n   under the center column. The Sidebar shell toggles the body attribute with\n   the panel open state. */\nbody[data-dsh-sidebar-collapsed] [data-slot=\"conversation.session.header\"] > header {\n  padding-right: 78px;\n}\n\nbody[data-dsh-sidebar-dragging] #root,\nbody[data-dsh-sidebar-dragging] #root > div[data-slot=\"root\"] > div > div:nth-child(2) {\n  transition: none;\n}\n\n@media (prefers-reduced-motion: reduce) {\n  #root,\n  #root > div[data-slot=\"root\"] > div > div:nth-child(2) {\n    transition: none;\n  }\n}\n";
+		const desktopSafeLayoutCss = "\n#root {\n  margin-right: 0;\n}\n#root > div[data-slot=\"root\"] > div > div:nth-child(2) {\n  margin-right: var(--dsh-sidebar-width, 0px);\n  transition: margin-right var(--ds-transition-duration-slow) var(--ds-ease-in-out), margin-bottom var(--ds-transition-duration-slow) var(--ds-ease-in-out);\n}\nbody[data-dsh-sidebar-dragging] #root > div[data-slot=\"root\"] > div > div:nth-child(2) {\n  transition: none;\n}\n@media (prefers-reduced-motion: reduce) {\n  #root > div[data-slot=\"root\"] > div > div:nth-child(2) {\n    transition: none;\n  }\n}\n";
 		const tagId = "dsh-better-sidebar/layout.css";
 		if (typeof document !== "undefined" && document.querySelector("style[data-plugin-css=" + JSON.stringify(tagId) + "]") === null) {
 			const tag = document.createElement("style");
 			tag.dataset.plugin = "dsh-better-sidebar";
 			tag.dataset.pluginCss = tagId;
-			tag.textContent = css;
+			tag.textContent = css + desktopSafeLayoutCss;
 			document.head.appendChild(tag);
 		}
 		//#endregion
diff --git a/lib/index.js b/lib/index.js
index b6ae4a6832ce5f01b419f4fc7f3432fa54afd91e..fd63077f96f0dd27c5503466f081660552d8b86f 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -1,6 +1,6 @@
 import { createRequire } from "node:module";
 import { mkdir, open, opendir, readFile, rename, rm, stat, writeFile } from "node:fs/promises";
-import { basename, dirname, extname, isAbsolute, join, resolve } from "node:path";
+import { basename, dirname, extname, isAbsolute, join, posix, win32 } from "node:path";
 import { WebSocket, WebSocketServer } from "ws";
 import z from "schemastery";
 import { createHash, randomUUID } from "node:crypto";
@@ -210,9 +210,10 @@ function parentOf(path) {
 	return parent === path ? void 0 : parent;
 }
 /** Normalize a caller-supplied path to an absolute, resolved path or throw fs-error. */
-function requireAbsolute(path) {
-	if (!path.startsWith("/") && !/^[A-Za-z]:[\\/]/.test(path)) throw new SidebarError("fs-error", `"${path}" is not an absolute path`, 400);
-	return resolve(path);
+function requireAbsolute(path, platform = process.platform) {
+	const pathApi = platform === "win32" ? win32 : posix;
+	if (!pathApi.isAbsolute(path)) throw new SidebarError("fs-error", `"${path}" is not an absolute path`, 400);
+	return pathApi.resolve(path);
 }
 /**
 * Whether `target` lies under `base` (or equals it), tolerant of separator
diff --git a/src/client/Sidebar.tsx b/src/client/Sidebar.tsx
index c450a36bc5f952c0c9fb1e9e602d4c356c5032c8..eed611fd5df9c3b504062f66d4c61871b5962755 100644
--- a/src/client/Sidebar.tsx
+++ b/src/client/Sidebar.tsx
@@ -32,7 +32,7 @@ import { IconCloseFill14, Tooltip } from '@deepseek-ai/dsh-client-ui-primitives'
 import type { Context, SidebarSessionList } from '../context-types.ts'
 import { appendToDraft } from './conversation-draft.ts'
 import {
-  BOTTOM_MIN, PANEL_MIN, agentUuidOf, firstLeaf, isAgentTabId, leafWithTab, migrateBottomTabs, moveTab, moveTabToEdge, openDiffTab,
+  BOTTOM_MIN, DRAFT_SESSION_ID, PANEL_MIN, agentUuidOf, firstLeaf, isAgentTabId, leafWithTab, migrateBottomTabs, moveTab, moveTabToEdge, openDiffTab,
   reconcileAgentTerminals,
   resizeSplitIn, setBottomHeight, setWidth, toggleBottomPanel, toggleExpanded, togglePanel,
   type DropZone, type SidebarState, type SidebarStore, type SidebarTab, type SplitNode,
@@ -149,7 +149,7 @@ export function Sidebar(props: { ctx: Context; store: SidebarStore }) {
     useCallback((callback: () => void) => store.subscribe(callback), [store]),
     useCallback(() => store.getSnapshot(), [store]),
   )
-  useEffect(() => { store.setSession(current) }, [current, store])
+  useEffect(() => { store.setSession(current ?? DRAFT_SESSION_ID) }, [current, store])
 
   const state = snapshot.state
   const sessionId = snapshot.sessionId
@@ -439,7 +439,7 @@ export function Sidebar(props: { ctx: Context; store: SidebarStore }) {
     // The bottom panel does not exist on narrow viewports (the two
     // workbenches merge into one panel), so the first-expansion auto
     // terminal is a desktop-only behavior.
-    if (narrow) return
+    if (narrow || current === undefined) return
     if (state === undefined) return
     const wasOpen = bottomWasOpenRef.current
     bottomWasOpenRef.current = state.bottomOpen
@@ -451,7 +451,7 @@ export function Sidebar(props: { ctx: Context; store: SidebarStore }) {
     // atomically so later expansions never repeat the auto-open.
     store.reduce(s => ({ ...s, activePane: firstLeaf(s.bottomSplits).id, bottomOpenedOnce: true }))
     ctx.betterSidebar?.openTab({ type: 'terminal' })
-  }, [state, store, ctx, narrow])
+  }, [state, store, ctx, narrow, current])
 
   // Panel drags: the right panel's width (left edge strip), the bottom
   // panel's height (top edge strip), and the shared corner (both at once).
diff --git a/src/client/layout.css b/src/client/layout.css
index 4b3a67779bf0cfd8957de3cc9f920b4386de6506..c7c35a22d2eabd2b9c00131b5e99904471eca6e0 100644
--- a/src/client/layout.css
+++ b/src/client/layout.css
@@ -1,16 +1,16 @@
 /**
  * Layout push: when a panel is open it OCCUPIES the layout instead of
- * floating over it — the app shell (#root, the AppFrame three-column grid)
- * gives up space. Only the center column is flexible (1fr), so the right
- * panel's width squeeze (margin-right on #root) lands exactly on the
- * conversation output and the input bar, like a VSCode sidebar.
+ * floating over it. The right panel pushes ONLY the AppFrame's center
+ * column; it must not shrink #root/the frame itself. AppFrame observes its
+ * own width and automatically collapses the left sidebar below 1024px, so a
+ * root margin would make opening this panel incorrectly trigger that mode.
  *
  * The bottom panel squeezes ONLY the center column — it must not cover the
  * app's own left sidebar or the right panel. DSH 0.1.x wraps slot hosts in
  * [data-slot] containers, so the AppFrame grid lives one level deeper:
  * #root > div[data-slot="root"] > div (the frame). Its grid items are
  * (frame > div:nth-child(1..3)): sidebarCol, centerCol, detailsCol, so the
- * vertical push lands on the center column alone. The fixed panels cover
+ * horizontal and vertical pushes land on the center column alone. The fixed panels cover
  * the vacated strips, which looks seamless because #root's background is
  * the theme base.
  *
@@ -19,18 +19,16 @@
  * slides on the same theme duration. Drags disable the transition so the
  * layout tracks the pointer.
  */
-#root {
-  margin-right: var(--dsh-sidebar-width, 0px);
-  transition: margin-right var(--ds-transition-duration-slow) var(--ds-ease-in-out);
-}
-
 /* The AppFrame's grid items are sidebarCol, centerCol, detailsCol (children
    1-3 of #root > div[data-slot="root"] > div) — nth-child(2) is the center
    column. A stretched grid item shrinks by its margins, so the conversation
    content (output + input bar) lifts without touching the sidebars. */
 #root > div[data-slot="root"] > div > div:nth-child(2) {
+  margin-right: var(--dsh-sidebar-width, 0px);
   margin-bottom: var(--dsh-sidebar-height, 0px);
-  transition: margin-bottom var(--ds-transition-duration-slow) var(--ds-ease-in-out);
+  transition:
+    margin-right var(--ds-transition-duration-slow) var(--ds-ease-in-out),
+    margin-bottom var(--ds-transition-duration-slow) var(--ds-ease-in-out);
 }
 
 /* When the sidebar is collapsed, the toggle cluster reclaims the top-right
@@ -46,13 +44,11 @@ body[data-dsh-sidebar-collapsed] [data-slot="conversation.session.header"] > hea
   padding-right: 78px;
 }
 
-body[data-dsh-sidebar-dragging] #root,
 body[data-dsh-sidebar-dragging] #root > div[data-slot="root"] > div > div:nth-child(2) {
   transition: none;
 }
 
 @media (prefers-reduced-motion: reduce) {
-  #root,
   #root > div[data-slot="root"] > div > div:nth-child(2) {
     transition: none;
   }
diff --git a/src/client/produced-files.ts b/src/client/produced-files.ts
index a0c2a7b4c207e547d02738d255a60c20a5c86978..174ced3ec51c6c57a880cccc4487e594b536ee3f 100644
--- a/src/client/produced-files.ts
+++ b/src/client/produced-files.ts
@@ -78,7 +78,7 @@ export function selectProducedFiles(owner: unknown): readonly string[] | null {
 
 /** Resolve a (possibly relative) path against the session cwd for the sidebar. */
 export function resolveSidebarPath(cwd: string | undefined, path: string): string {
-  const absolute = path.startsWith('/') || /^[A-Za-z]:[\\/]/.test(path)
+  const absolute = path.startsWith('/') || path.startsWith('\\\\') || /^[A-Za-z]:[\\/]/.test(path)
   if (absolute) return path
   const base = cwd ?? ''
   if (base === '') return path
diff --git a/src/client/state.ts b/src/client/state.ts
index 8656ddcd9be1250dc4b04276fbc600689c66e292..11d105ee48461ff9240bca4f6950d030b46c34a1 100644
--- a/src/client/state.ts
+++ b/src/client/state.ts
@@ -727,6 +727,7 @@ export function reconcileAgentTerminals(
 // ── The per-session store ──────────────────────────────────────────────────
 
 const STORAGE_PREFIX = 'dsh-sidebar:v1'
+export const DRAFT_SESSION_ID = '__dsh_better_sidebar_draft__'
 
 /** Immutable snapshot handed to React (replaced only on real changes). */
 export interface SidebarSnapshot {
@@ -775,6 +776,9 @@ function loadState(sessionId: string, prefs: SidebarPrefs): SidebarState {
   const width = viewport === undefined
     ? PANEL_DEFAULT
     : defaultWidthFor(viewport, prefs.defaultWidthPercent)
+  if (sessionId === DRAFT_SESSION_ID) {
+    return makeDefaultState(width, false, prefs.tabsEnabled['explorer'] !== false)
+  }
   const openByDefault = prefs.openByDefault && (viewport === undefined || !isNarrowWidth(viewport))
   return makeDefaultState(width, openByDefault, prefs.tabsEnabled['explorer'] !== false)
 }
diff --git a/src/fs-tree.ts b/src/fs-tree.ts
index 437cff71be28ff9f56ea120b3547c37a02dcfa28..36b6ede43f49c97ba7bd913e6eb83478aa035436 100644
--- a/src/fs-tree.ts
+++ b/src/fs-tree.ts
@@ -6,7 +6,7 @@
  * dirent says, keeping the read cheap for arbitrarily large levels.
  */
 import { opendir } from 'node:fs/promises'
-import { basename, dirname, join, resolve } from 'node:path'
+import { basename, dirname, join, posix, win32 } from 'node:path'
 import { SidebarError } from './wire.ts'
 
 /** One explorer row. */
@@ -81,11 +81,15 @@ export function parentOf(path: string): string | undefined {
 }
 
 /** Normalize a caller-supplied path to an absolute, resolved path or throw fs-error. */
-export function requireAbsolute(path: string): string {
-  if (!path.startsWith('/') && !/^[A-Za-z]:[\\/]/.test(path)) {
+export function requireAbsolute(
+  path: string,
+  platform: NodeJS.Platform = process.platform,
+): string {
+  const pathApi = platform === 'win32' ? win32 : posix
+  if (!pathApi.isAbsolute(path)) {
     throw new SidebarError('fs-error', `"${path}" is not an absolute path`, 400)
   }
-  return resolve(path)
+  return pathApi.resolve(path)
 }
 
 /**
